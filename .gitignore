# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
# 双基座构建的临时产物（正式 /doupo/ 与体验 /doupo-test/ 各一份，只用于本地对比验证）
# 用 dist-*/ 通配而不是逐个列举：这些都是**构建产物**，而本仓库是**公开仓库**，
# 漏列一个（dist-test-recheck/、dist-vtmp/ 就这么漏过）就会在一次 `git add -A` 里被推上去。
dist-*/
*.local
# 同类：双基座对比用的**构建产物**，只是目录名不带 dist- 前缀（.bridge-prod/ = /doupo/ 那份、
# .bridge-test/ = /doupo-test/ 那份，各 13MB，197 个文件，内容就是 index.html + assets/*.js）。
# 上面 dist-*/ 那条的教训在这儿原样重演了一次：名字不匹配 ⇒ 一次 `git add -A` 就是 26MB 进公开仓库。
.bridge-prod/
.bridge-test/

# 密钥/备份文件，不进仓库
.env
*.bak
*.bak2
*.bak3
*.bak4
*.bak5
*.bak6
*.bak7
*.bak8
# 单文件回滚副本（部署/改动前 `cp` 出来的那一份）。
# 上面那串 *.bakN 只拦得住纯数字后缀，`xxx.ts.bak-before-v164-20260923-204328`
# 这种**带日期**的拦不住 —— 而它恰恰是后来最常用的一种命名。用 *.bak-* 一把锁住。
*.bak-*
# 素材全尺寸备份（159MB，v1.26 瘦身前的 1024 PNG）与抠图工具的逐文件回滚副本。
# 这是**公开仓库**：目录一直没进过 git，只是没用 ignore 锁住 —— 一次 `git add -A` 就会全推进去。
.backup-sprites-png/
*.pocketbak

# 同类：**美术素材的中间产物**，与运行无关，全是"生图/改图过程里留下的那一版"。
# 判据是「有没有被源码引用」——`boss2/*.webp` 有（monsters.ts 的 import.meta.glob），
# 下面这些一个都没有：`generated/` 是各轮生图的候选图，`poster-v2/v3.png` 是海报定稿前
# 的两版，`sprites/src/` 更是路径写错建出来的嵌套空壳（`sprites/src/assets/sprites/…`）。
# 一并锁住：这批东西合计 7MB+，且每做一次美术就会再多几个。
.backup-sprites-webp/
src/assets/sprites/generated/
src/assets/sprites/src/
src/assets/sprites/link/poster-v*.png

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
